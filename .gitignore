.dart_tool/
.packages
.flutter-plugins
.flutter-plugins-dependencies
.metadata
build/
coverage/

# IDE
.idea/
.vscode/

# Platform folders at repo root (keep the package itself pure).
/android/
/ios/
/linux/
/macos/
/web/
/windows/

# Lock file is usually not committed for packages
pubspec.lock
