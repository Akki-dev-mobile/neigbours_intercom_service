name: onegate_neighbours
description: Plug-and-play Neighbours feature for OneGate-compatible Flutter apps.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  onegate_feature_core:
    path: ../onegate_feature_core
  onegate_intercom:
    path: ../onegate_intercom

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
